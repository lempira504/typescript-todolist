# run tsc to generate js files in /dist
npx tsc or npx tsc -w

# start package.json
npm init -y

# install lite server for live changes
npm install lite-server 

# in script package.json
"start": "lite-server"
    # run 
    npm start